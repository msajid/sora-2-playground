# Required: Uncomment and add your OpenAI API key
# OPENAI_API_KEY=

# Optional: Uncomment to customize backend deployments
# APP_PASSWORD=
# NEXT_PUBLIC_FILE_STORAGE_MODE=fs  # or indexeddb
# OPENAI_API_BASE_URL=

# Optional: Enable frontend-only mode (users supply their API key in-browser).
# When true the app runs purely in the browser, videos are stored in IndexedDB,
# and password protection is disabled. Use `pnpm build:frontend` to produce a static build.
# NEXT_PUBLIC_ENABLE_FRONTEND_MODE=true
